module.exports = {
    command: 'defa-auth',
    aliases: ['dauth,authc'],
    description: 'Defender 001 ethical operations tools 1',
    category: 'xadon',
    
    execute: async(sock, m, { reply }) => {
      if (isGroup)
          return reply('Good day group this is XADON AI\n*✨Welcome to the Cybershield squad amazing Defense technology🚀*')
    

⚙️ *Top 50 Termux Tools (Ethical Use)*

1. *Nmap*  
   `pkg install nmap`  
   `nmap <target-ip>`

2. *Hydra*  
   `pkg install hydra`  
   `hydra -l user -P passlist.txt <target> ssh`

3. *Sqlmap*  
   `pkg install sqlmap`  
   `sqlmap -u <url> --dbs`

4. *Metasploit*  
   `pkg install unstable-repo && pkg install metasploit`  
   `msfconsole`

5. *Nikto*  
   `pkg install nikto`  
   `nikto -h <website>`

6. *John the Ripper*  
   `pkg install john`  
   `john --wordlist=passwords.txt hashes.txt`

7. *Aircrack-ng* *(needs root)*  
   `pkg install aircrack-ng`

8. *Tool-X*  
   `git clone https://github.com/rajkumardusad/Tool-X`  
   `cd Tool-X && bash install.sh`

9. *ReconDog*  
   `git clone https://github.com/s0md3v/ReconDog`  
   `cd ReconDog && python2 dog.py`

10. *Weeman (Phishing)*  
    `git clone https://github.com/samyoyo/weeman.git`

11. *Camphish*  
    `git clone https://github.com/techchipnet/CamPhish`  
    `bash camphish.sh`

12. *Saycheese*  
    `git clone https://github.com/thelinuxchoice/saycheese`  
    `bash saycheese.sh`

13. *Infoga*  
    `git clone https://github.com/m4ll0k/Infoga.git`
     `cd Infoga && python2 infoga.py`

14. *RED_HAWK*  
    `git clone https://github.com/Tuhinshubhra/RED_HAWK`  
    `cd RED_HAWK && php rhawk.php`

15. *IP-Tracer*  
    `git clone https://github.com/rajkumardusad/IP-Tracer`  
    `cd IP-Tracer && bash install.sh`

16. *Locator*  
    `git clone https://github.com/thewhiteh4t/Locator`  
    `cd Locator && bash locator.sh`

17. *SocialFish*  
    `git clone https://github.com/UndeadSec/SocialFish`  
    *(Check for updated forks)*

18. *Ghost Framework*  
    `git clone https://github.com/EntySec/ghost`  
    `cd ghost && python3 ghost.py`

19. *Zphisher*  
    `git clone https://github.com/htr-tech/zphisher`  
    `cd zphisher && bash zphisher.sh`

20. *Brute Force Gmail*  
    `git clone https://github.com/JamesAndresCM/Gmail-Hack`  
    *(Educational use only)*

21. *D-TECT*  
    `git clone https://github.com/shawarkhanethicalhacker/D-TECT-1`  
    `cd D-TECT-1 && python2 d-tect.py`

22. *XSStrike*  
    `git clone https://github.com/s0md3v/XSStrike`  
    `cd XSStrike && python3 xsstrike.py`

23. *Hammer (DDoS Tool)*  
    `git clone https://github.com/cyweb/hammer`  
    `cd hammer && python hammer.py`

24. *T-Spy (Android)*  
    `git clone https://github.com/DarkSecDevelopers/HiddenEye` *(used to be called T-Spy)*
25. *Kali Nethunter*  
    `git clone https://github.com/Hax4us/Nethunter-In-Termux`  
    `cd Nethunter-In-Termux && bash kalinethunter`

26. *Hash-Buster*  
    `git clone https://github.com/UltimateHackers/Hash-Buster`  
    `cd Hash-Buster && python hash.py`

27. *Sherlock*  
    `git clone https://github.com/sherlock-project/sherlock`  
    `cd sherlock && python3 sherlock.py username`

28. *Termux-Banner*  
    `git clone https://github.com/Bhai4You/Termux-Banner`  
    `cd Termux-Banner && bash requirements.sh && bash t-ban.sh`

29. *Wifi-Hacker* *(Root required)*  
    *(Various GitHub scripts available)*

30. *NetHunter Kex* (GUI access)  
    `apt install tigervnc`
31. *ReconDog*  
- Description: Automated reconnaissance tool for scanning and information gathering.  
- Install & run:  
  ```
  git clone https://github.com/UltimateHackers/ReconDog.git
  cd ReconDog
  chmod +x dog.py
  python dog.py
  ```

32. *Fsociety*  
- Description: All-in-one hacking framework with multiple tools for pentesting.  
- Install & run:  
  ```
  git clone https://github.com/Manisso/fsociety.git
  cd fsociety
  chmod +x fsociety.py
  python fsociety.py
  ```

33. *Wireshark*  
- Description: Network protocol analyzer for network troubleshooting and analysis.  
- Install:  
  ```
  pkg install wireshark
  wireshark
  ```

34. *Aircrack-ng*  
- Description: Suite of tools to assess Wi-Fi network security.  
- Install:  
  ```
  pkg install aircrack-ng
  aircrack-ng
  ```

35. *SQLmap*  
- Description: Automatic SQL injection tool.  
- Install:  
  ```
  pkg install sqlmap
  sqlmap -h
  ```

36. *Metasploit Framework*  
- Description: Powerful penetration testing framework.  
- Install:  
  ```
  pkg install unstable-repo
  pkg install metasploit
  msfconsole
  ```

37. *Nmap*  
- Description: Network scanner for discovering devices and services.  
- Install:  
  ```
 pkg install nmap
  nmap -h
  ```

38. *Hydra*  
- Description: Network logon cracker supporting many protocols.  
- Install:  
  ```
  pkg install hydra
  hydra -h
  ```

39. *John the Ripper*  
- Description: Password cracker tool.  
- Install:  
  ```
  pkg install john
  john --help
  ```

40. *Nikto*  
- Description: Web server scanner to find vulnerabilities.  
- Install:  
  ```
  pkg install nikto
  nikto -h
  ```

41. *Zphisher*  
- Description: Phishing tool for automated fake login page generation.  
- Install & run:  
  ```
  git clone https://github.com/htr-tech/zphisher.git
  cd zphisher
  bash zphisher.sh
  ```

42. *SpamSms*  
- Description: SMS spamming tool (ethical use only).  
- Install & run:  
  ```git clone https://github.com/1N3/SpamSms.git
  cd SpamSms
  python3 spam.py```

43. *Termux-api*  
- Description: Access Android device features via Termux commands.  
- Install:  
  ```pkg install termux-api```

44. *Curl*  
- Description: Data transfer tool supporting many protocols.  
- Install:  
  ```pkg install curl
  curl --help```

45. *Wifite*  
- Description: Automated wireless attack tool.  
- Install & run:  
  ```git clone https://github.com/derv82/wifite2.git
  cd wifite2
  python wifite.py```

46. *BlackBox*
- Description: All-in-one penetration testing framework for Termux.  
- Install & run:  
  ```git clone https://github.com/Ranginang67/blackbox.git
  cd blackbox
  bash blackbox.sh```

47. *Routersploit*  
- Description: Exploitation framework for embedded devices.  
- Install:  
  ```pkg install python
  pip install routersploit
  rsf```

48. *Ghost Framework*  
- Description: Remote administration framework for Android devices.  
- Install & run:  
  ```git clone https://github.com/Screetsec/Ghost.git
  cd Ghost
  chmod +x ghost.py
  python ghost.py```

49. *CamPhish*  
- Description: Phishing tool that sends fake camera permissions prompt.  
- Install & run:  
  ```git clone https://github.com/UndeadSec/CamPhish.git
  cd CamPhish
  bash camphish.sh```

50. *SayCheese*  
- Description: Tool to capture pictures remotely from a target device’s camera (ethical use only).  
- Install & run:  
  ```git clone https://github.com/thelinuxchoice/saycheese.git
  cd saycheese
  bash saycheese.sh```
`DEFENDER 001`